---
import LetsTalk from "./LetsTalk.astro";
---

<LetsTalk
  title="Contáctanos"
  subtitle="Hablemos de tu próximo proyecto metalmecánico"
  color="white"
  id="contact"
  showFooterContent="true"
  mostrarFormulario={true}
>
  <div slot="footer" class="w-full border-t border-white/10 pt-4 pb-2 px-6">
    <div class="flex flex-wrap justify-center gap-x-8 gap-y-1 text-xs text-white/50">
      <span>contacto@metalico.mx</span>
      <span>55 8108 2193</span>
      <span>CDMX, México</span>
      <span>Lun–Vie 9:00–18:00</span>
    </div>
  </div>
  <!-- <img slot="contain" src="logo_metalico.png"/> -->
  <img
    slot="background"
    alt=""
    class="h-full w-full object-cover object-center"
    src="bg-steel-black.avif"
    id="bg-image-contact"
  />
</LetsTalk>
<script>
  function isEdge() {
    if (
      navigator.userAgent.indexOf("Edge") > -1 ||
      navigator.userAgent.indexOf("Edg") > -1
    ) {
      return true;
    } else {
      return false;
    }
  }

  let imageElement = document.querySelector(
    "#bg-image-contact",
  ) as HTMLImageElement;

  if (isEdge()) {
    imageElement.src = "contact-bg.avif";
  } else {
    imageElement.src = "contact-bg.avif";
  }
</script>
